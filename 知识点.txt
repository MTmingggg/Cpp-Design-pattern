1.简单工厂模式和工厂方法模式的适用场景对比
	简单工厂：工厂负责创建的类少，不会造成工厂逻辑复杂，代码出错率低。客户端只知道传入参数，对创建对象并不关心。
	工厂方法：客户端不需要知道创建对象的类名。抽象工厂通过子类创建对象。

2.多线程概念
	竞争资源：同时成为几个线程访问的操作数
	解决方案：加锁，这个线程占用了，其他线程在门外等着

3.多线程情况下单例用懒汉式还是饿汉式？为什么？
	用饿汉式。懒汉式是进去再new，可能同时，因此线程不安全。饿汉式在线程开启之前就被new好了，因此线程安全。
	非要用懒汉式的话需要加个锁。